// COMPREHENSIVE CYBERSECURITY ARTICLES - FULL VERSION
// This file contains complete, detailed articles with comprehensive explanations

const comprehensiveArticles = {
    // Keep existing articles (DNS, AD, VPN, Linux, TCP/IP) as they are already comprehensive
    // These will be copied from the original file
    
    // Then add the FULL versions of the new articles below
};

// Export for browser
if (typeof window !== 'undefined') { 
    window.comprehensiveArticles = comprehensiveArticles; 
}
